# OmniOS dashboard — public client configuration only.
#
# These two values are PUBLIC by design: the publishable (anon) key is safe in a
# browser bundle because every query runs through the signed-in user's session and
# row-level security. Never add a service-role / secret key to this app: it would
# bypass RLS and the whole point of the dashboard is that it cannot.
NEXT_PUBLIC_SUPABASE_URL=https://bvxjthifyekabpkmpcji.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_fcHQPvWdgl962E2LV3H_8w_U8ePMlkW
